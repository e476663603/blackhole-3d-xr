# Task Summary — GitHub Pages 部署

## Objective
通过 GitHub Pages 直接访问 WebAR 页面，无需本地服务/内网穿透。

## What Was Done

1. **发现 Taro H5 的 copy 插件 bug**：当源目录为空时 webpack 报错 `unable to locate`。用 `.gitkeep` 占位解决了。

2. **添加 images 目录到构建**：在 `public/images/` 放入 Hiro.png，构建时复制到 `dist/images/`。

3. **修复相对路径**：将 `config/index.ts` 的 `h5.publicPath` 从 `/` 改为 `./`，让 JS/CSS 引用变为相对路径，这样在任意子路径下都能正常工作。

4. **手动迁移构建产物**：
   - 构建输出在 `blackhole-3d-xr/dist/`
   - images/models 被移动到 dist（因为 copy 插件输出到上级目录）
   - deploy/ repo 独立管理 gh-pages 分支，手动推送

5. **已推送到 GitHub**：
   - 仓库：`https://github.com/e476663603/blackhole-3d-xr`
   - 分支：`gh-pages`（force pushed）commit `a2ecf49`

## 最终文件结构
```
gh-pages branch (deploy/):
  index.html          (相对路径引用 ./js/ ./css/)
  css/
  js/
  images/Hiro.png
  models/test.fbx
```

## 访问地址
→ **https://e476663603.github.io/blackhole-3d-xr/**

手机访问这个地址，点击"启动 AR"按钮，摄像头对准 Hiro 识别图即可看到 3D 模型叠加效果。

## 注意事项
- 模型路径 `/models/test.fbx` 在 GitHub Pages 上解析为仓库根目录，相对 deploy/ 目录结构是正确的
- 页面通过相对路径 `./js/...` 加载 JS/CSS，无需配置 base URL
- 每次修改代码后需重新 `npm run build:h5`，然后手动更新 deploy/ 并 push