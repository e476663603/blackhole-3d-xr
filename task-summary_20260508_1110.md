# 动态版本号实现 - 解决缓存问题

## Objective
用户反馈"每次更新带版本号这样避免缓存"。之前用户遇到过浏览器缓存旧版本代码的问题（v4 部署后加载的却是旧代码）。

## Key Reasoning

### 问题根因
- GitHub Pages 的 HTML 文件（index.html）无法带 hash
- 浏览器可能缓存 HTML，导致加载旧的 JS/CSS
- 用户刷新页面时看不到最新代码

### 解决方案
1. 在 webpack defineConstants 中注入 VERSION 常量
2. VERSION 格式：`年.月.日.时分`（如 `2026.05.08.1110`）
3. 在组件标题栏显示版本号
4. 用户一眼就能判断是否加载了最新版本

### 技术实现
- 修改 `config/index.ts`：在 defineConstants 中添加 VERSION
- 修改 `src/pages/viewer/index.tsx`：
  - 添加 `declare const VERSION: string`
  - 标题改为 `{typeof VERSION !== 'undefined' ? VERSION : 'dev'}`

## Conclusions

### 部署结果
- commit: `3dd8ec2`
- 版本号: `2026.05.08.1110`
- 访问: https://e476663603.github.io/blackhole-3d-xr/

### 版本号格式说明
- `2026.05.08.1110` = 年.月.日.时分
- 每次构建自动更新
- 开发环境显示 'dev'

### 修改文件
1. `config/index.ts` - 添加 VERSION 常量定义
2. `src/pages/viewer/index.tsx` - 声明并使用 VERSION

### 后续使用
- 用户刷新页面后查看标题栏版本号
- 如果版本号是最新的，说明缓存已刷新
- 如果版本号旧，需要强制刷新（Ctrl+F5 或清缓存）
