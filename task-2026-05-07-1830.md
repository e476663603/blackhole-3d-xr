# Task: WebAR 移除 AR.js，改用纯 Three.js + getUserMedia

## Objective
修复手机端 "Cannot read properties of undefined (reading 'init')" 错误，原因是 AR.js CDN 在手机上加载失败。用户要求彻底移除 AR.js，重新调整逻辑。

## Key Reasoning
1. **根因分析**: 代码通过 CDN 动态加载 AR.js (`https://cdn.jsdelivr.net/npm/ar.js@2.2.2/three.js/build/ar.js`)，但 CDN 在国内手机网络环境下被墙或超时，导致 `ARjs` 为 undefined，调用 `ARjs.init()` 时报错
2. **方案选择**: 用户明确要求不用 AR.js → 采用纯原生 API 方案
   - `navigator.mediaDevices.getUserMedia({ video: { facingMode: 'environment' } })` 获取后置摄像头
   - 创建 `<video>` 元素播放视频流作为 CSS 背景（z-index: 0）
   - Three.js WebGLRenderer 渲染 3D 场景叠加在视频之上（z-index: 1, pointer-events: none）
   - FBXLoader 加载 test.fbx 模型，自动居中缩放
3. **部署问题修复**: 
   - gh-pages 分支根目录文件结构错误（之前文件在 dist/ 子目录下）
   - 通过 git worktree + force push 修复
   - webpack copy 插件未正确复制 public/images 和 public/models 到 dist，手动复制补充

## Changes Made
- **src/pages/viewer/index.tsx**: 完全重写
  - 移除 AR.js CDN 加载、ARjs.init()、MarkerControls
  - 新增 getUserMedia 摄像头获取逻辑
  - video 元素作为固定背景层
  - Three.js 独立渲染 3D 场景
  - 模型直接显示在画面中心（无 marker 检测）
  - 增强清理逻辑（停止 MediaStream tracks、移除 video 元素等）

## Conclusions
- 构建成功（webpack 5.78.0, 9.87s）
- 部署到 gh-pages 分支（commit c84bb63）
- 源码推送到 main 分支（commit f5793a1）
- 访问地址: https://e476663603.github.io/blackhole-3d-xr/
- 下一步: 用户在安卓手机上测试验证摄像头启动和模型显示

## Files Modified
- `src/pages/viewer/index.tsx` — 完全重写，移除 AR.js 依赖
