# WebXR Image Tracking AR 部署

## Objective
将 AR 项目改回 WebXR 方案，使用 `image-tracking` feature 实现识别图检测和 3D 模型定位

## Key Reasoning

### 用户反馈
- 用户要求使用 WebXR 写法，之前的错误可能是测试问题
- 需要图像追踪和识别图定位功能

### 技术方案
使用 WebXR Device API 的 `image-tracking` feature：
- `navigator.xr.requestSession('immersive-ar', { trackedImages: [...] })`
- `frame.getImageTrackingResults()` 获取追踪结果
- 模型位置根据 `result.imageSpace` 的 pose 更新

### 浏览器兼容性
- Chrome Android 91+（需启用 flags）
- Samsung Internet（部分版本）
- Safari iOS ❌ 不支持 image-tracking

## Conclusions

### 部署结果
- 已推送到 gh-pages 分支：commit `1400d62`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

### 修改文件
1. `src/pages/viewer/index.tsx` — WebXR Image Tracking 实现
   - 加载识别图 ImageBitmap
   - 请求 immersive-ar session 带 trackedImages 参数
   - 渲染循环中获取 image tracking 结果
   - 更新模型位置到识别图 pose
2. `src/pages/viewer/viewer.scss` — DOM Overlay 样式

### 测试要求
1. Chrome Android 在 chrome://flags 启用 "WebXR Incubations"
2. 准备 Hiro marker 识别图
3. 对准识别图后模型会自动定位

### 技术迭代
- v3: WebRTC + Three.js — 通用但无 marker detection
- v4: WebXR Image Tracking — 标准 AR 方案，有 marker detection

## Open Issues
- 用户手机是否支持 WebXR Image Tracking 待验证
- 如仍报错，需要具体错误信息进一步排查
