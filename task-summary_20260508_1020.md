# Camera AR v3 - WebRTC + Three.js Universal Solution

## Objective
将 WebXR AR 项目改为 Camera AR 方案，解决手机浏览器不支持 WebXR Device API 的问题（`InvalidStateError`）

## Key Reasoning

### 问题诊断
- 用户手机访问 https://e476663603.github.io/blackhole-3d-xr/ 报错 `InvalidStateError`
- 根因：`navigator.xr.requestSession('immersive-ar', ...)` 在用户设备上失败
- 国内安卓浏览器普遍不支持 WebXR immersive-ar 模式

### 技术方案选择
放弃 WebXR Device API，改用 **WebRTC + Three.js** 纯 H5 方案：
- `getUserMedia` 获取摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方

### 兼容性优势
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用
- ❌ 不依赖 WebXR Device API

## Conclusions

### 部署结果
- 构建成功
- 部署到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

### 修改文件
1. `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
   - `getUserMedia` 请求后置摄像头
   - Three.js 场景初始化（透明背景）
   - FBXLoader 加载模型
   - 渲染循环 + 自动旋转动画
2. `src/pages/viewer/viewer.scss` — video/canvas 叠加布局

### 测试步骤
1. 手机访问 GitHub Pages 链接
2. 点击「启动 AR」
3. 允许摄像头权限
4. 看到 3D 模型叠加在摄像头画面上缓慢旋转

## Previous Iterations (Context)
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案
