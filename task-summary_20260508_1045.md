# WebXR v5 多策略部署

## Objective
重写 WebXR viewer 组件为 v5 版本，解决 v4 黑屏无错误信息的问题。用户坚持使用 WebXR 方案。

## Key Reasoning

### 用户反馈
- v4 部署后点击"启动 AR"黑屏，没有调用摄像头
- 用户明确要求坚持 WebXR 方案，不要换其他方案
- 用户手机是华为/荣耀型号

### v4 失败原因分析
1. `requestSession('immersive-ar')` 调用静默失败（错误被 catch 但显示不清晰）
2. 手机浏览器不支持 immersive-ar 模式
3. image-tracking 是实验性 feature，需要 chrome://flags 启用
4. 没有做任何降级处理

### v5 改进点
1. **详细错误展示** - 所有错误显示在屏幕上，不再静默失败
2. **自动检测** - 页面加载时检测 navigator.xr 存在性、immersive-ar 支持、inline 支持
3. **Debug 日志** - 实时显示在页面底部，包含 User Agent 信息
4. **三策略降级**:
   - 策略1: immersive-ar + image-tracking (最佳)
   - 策略2: immersive-ar 基础 (无 marker)
   - 策略3: inline + dom-overlay (内联)
5. **状态机清晰** - idle → checking → loading → active/error/not-supported

## Conclusions

### 部署结果
- 已推送到 gh-pages 分支：commit `11bb54a`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

### 修改文件
1. `src/pages/viewer/index.tsx` — 完全重写为 v5 多策略实现
2. `src/pages/viewer/viewer.scss` — 更新样式

### 技术迭代路径
- v1: AR.js + Three.js — CDN 被墙
- v2: JSARToolKit5 — 复杂
- v2.5: WebXR Device API — 不支持
- v3: WebRTC + Three.js — 通用但用户不要
- v4: WebXR Image Tracking — 黑屏
- v5: WebXR Multi-Strategy ✅ 已部署

### 下一步
- 等待用户测试并反馈具体错误信息
- 根据错误信息进一步调试
