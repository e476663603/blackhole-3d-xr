# 2026-05-08 工作日志

### 08:17  -  AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常

### 10:27 - WebXR Image Tracking v4 部署完成

**用户反馈**：用户要求改回 WebXR 方案，使用 `image-tracking` feature 实现识别图检测

**技术实现**：
- WebXR Device API `image-tracking` feature
- `navigator.xr.requestSession('immersive-ar', { trackedImages: [{ image: ImageBitmap, widthInMeters: 0.15 }] })`
- `frame.getImageTrackingResults()` 获取追踪状态
- 模型位置根据 `result.imageSpace` 的 pose 更新

**浏览器兼容性**：
- Chrome Android 91+（需在 chrome://flags 启用 "WebXR Incubations"）
- Samsung Internet 部分版本支持
- Safari iOS ❌ 不支持 image-tracking

**部署结果**：
- 已推送到 gh-pages 分支：commit `1400d62`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebXR Image Tracking 实现
- `src/pages/viewer/viewer.scss` — DOM Overlay 样式

**技术迭代路径更新**：
- v3: WebRTC + Three.js — 通用但无 marker detection
- v4: WebXR Image Tracking — 标准 AR 方案，有 marker detection ✅ 已部署

**测试要求**：
1. Chrome Android 在 chrome://flags 启用 "WebXR Incubations"
2. 准备 Hiro marker 识别图（已部署在 images/Hiro.png）
3. 对准识别图后模型会自动定位到 marker 上

**待验证**：用户手机是否支持 WebXR Image Tracking，需实测确认
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常

### 10:27 - WebXR Image Tracking v4 部署完成 & 测试失败

**用户反馈**：用户要求改回 WebXR 方案，使用 `image-tracking` feature 实现识别图检测

**技术实现**：
- WebXR Device API `image-tracking` feature
- `navigator.xr.requestSession('immersive-ar', { trackedImages: [{ image: ImageBitmap, widthInMeters: 0.15 }] })`
- `frame.getImageTrackingResults()` 获取追踪状态
- 模型位置根据 `result.imageSpace` 的 pose 更新

**部署结果**：
- 已推送到 gh-pages 分支：commit `1400d62`

### 10:30 - v4 测试结果：黑屏，无摄像头调用

**用户设备信息**（从截图判断）：
- 华为/荣耀手机（从 UI 风格和状态栏判断）
- 使用自带浏览器或 Chrome 访问 `76663603.github.io`
- 页面显示 "WebXR Image Tracking" 标题和"请瞄准并对准 Hiro 识别图"
- **点击启动 AR 后黑屏，完全没有调用摄像头**

**问题确认**：
1. WebXR `requestSession('immersive-ar')` 调用静默失败
2. 用户手机（华为/荣耀）的浏览器 **不支持 WebXR immersive-ar**
3. 即使是 Chrome Android，`image-tracking` 也是实验性功能，需手动开启 flags
4. 国产手机浏览器基本都不支持 WebXR AR

**结论**：WebXR 方案在用户设备上不可行，必须回到 WebRTC 方案

**下一步计划（v5）**：
- 回到 WebRTC Camera + Three.js 基础方案（摄像头 100% 可调用）
- 加入 JSARToolKit5 或自研 canvas 图像特征匹配实现 marker 检测
- 确保：① 摄像头能打开 ② 能检测 Hiro marker ③ 模型定位到 marker 位置
## 2026-05-08 Session Notes

### WebXR Blackhole AR Viewer - v5 Multi-Strategy Deployed
- Project repo: `e476663603/blackhole-3d-xr` on GitHub, deployed via gh-pages
- Live URL: https://e476663603.github.io/blackhole-3d-xr/
- User (Adam) is building a blackhole 3D AR viewer and insists on WebXR approach

### Iteration History
- v1: AR.js + Three.js — CDN blocked in China
- v2: JSARToolKit5 — too complex
- v2.5: WebXR Device API — initially seemed unsupported
- v3: WebRTC + Three.js — universal but user rejected (wants WebXR)
- v4: WebXR Image Tracking — deployed but black screen, no error shown, camera not invoked
- v5: WebXR Multi-Strategy — **deployed** (commit `11bb54a`), key improvements:
  - All errors displayed on screen (no more silent black screen)
  - Auto-detect WebXR support on page load
  - Debug log panel at bottom showing step-by-step execution + User Agent
  - Three-strategy fallback: immersive-ar+image-tracking → immersive-ar basic → inline
  - Clear state machine: idle → checking → loading → active/error/not-supported

### User Context
- Adam's phone: Huawei/Honor brand
- Strong preference for WebXR approach; rejected WebRTC fallback suggestion
- Awaiting test results from v5 — need screenshot with error details to continue debugging

### Technical Notes
- Files modified in v5: `src/pages/viewer/index.tsx` (full rewrite), `src/pages/viewer/viewer.scss`
- WebXR image-tracking is experimental, requires chrome://flags on most browsers
- immersive-ar requires HTTPS + compatible browser (Chrome Android, Samsung Internet, etc.)
# 2026-05-08 工作日志

### 08:17  -  AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常

### 10:27 - WebXR Image Tracking v4 部署完成

**用户反馈**：用户要求改回 WebXR 方案，使用 `image-tracking` feature 实现识别图检测

**技术实现**：
- WebXR Device API `image-tracking` feature
- `navigator.xr.requestSession('immersive-ar', { trackedImages: [{ image: ImageBitmap, widthInMeters: 0.15 }] })`
- `frame.getImageTrackingResults()` 获取追踪状态
- 模型位置根据 `result.imageSpace` 的 pose 更新

**浏览器兼容性**：
- Chrome Android 91+（需在 chrome://flags 启用 "WebXR Incubations"）
- Samsung Internet 部分版本支持
- Safari iOS ❌ 不支持 image-tracking

**部署结果**：
- 已推送到 gh-pages 分支：commit `1400d62`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebXR Image Tracking 实现
- `src/pages/viewer/viewer.scss` — DOM Overlay 样式

**技术迭代路径更新**：
- v3: WebRTC + Three.js — 通用但无 marker detection
- v4: WebXR Image Tracking — 标准 AR 方案，有 marker detection ✅ 已部署

**测试要求**：
1. Chrome Android 在 chrome://flags 启用 "WebXR Incubations"
2. 准备 Hiro marker 识别图（已部署在 images/Hiro.png）
3. 对准识别图后模型会自动定位到 marker 上

**待验证**：用户手机是否支持 WebXR Image Tracking，需实测确认
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常

### 10:27 - WebXR Image Tracking v4 部署完成 & 测试失败

**用户反馈**：用户要求改回 WebXR 方案，使用 `image-tracking` feature 实现识别图检测

**技术实现**：
- WebXR Device API `image-tracking` feature
- `navigator.xr.requestSession('immersive-ar', { trackedImages: [{ image: ImageBitmap, widthInMeters: 0.15 }] })`
- `frame.getImageTrackingResults()` 获取追踪状态
- 模型位置根据 `result.imageSpace` 的 pose 更新

**部署结果**：
- 已推送到 gh-pages 分支：commit `1400d62`

### 10:30 - v4 测试结果：黑屏，无摄像头调用

**用户设备信息**（从截图判断）：
- 华为/荣耀手机（从 UI 风格和状态栏判断）
- 使用自带浏览器或 Chrome 访问 `76663603.github.io`
- 页面显示 "WebXR Image Tracking" 标题和"请瞄准并对准 Hiro 识别图"
- **点击启动 AR 后黑屏，完全没有调用摄像头**

**问题确认**：
1. WebXR `requestSession('immersive-ar')` 调用静默失败
2. 用户手机（华为/荣耀）的浏览器 **不支持 WebXR immersive-ar**
3. 即使是 Chrome Android，`image-tracking` 也是实验性功能，需手动开启 flags
4. 国产手机浏览器基本都不支持 WebXR AR

**结论**：WebXR 方案在用户设备上不可行，必须回到 WebRTC 方案

**下一步计划（v5）**：
- 回到 WebRTC Camera + Three.js 基础方案（摄像头 100% 可调用）
- 加入 JSARToolKit5 或自研 canvas 图像特征匹配实现 marker 检测
- 确保：① 摄像头能打开 ② 能检测 Hiro marker ③ 模型定位到 marker 位置
## 2026-05-08 Session Notes

### WebXR Blackhole AR Viewer - v5 Multi-Strategy Deployed
- Project repo: `e476663603/blackhole-3d-xr` on GitHub, deployed via gh-pages
- Live URL: https://e476663603.github.io/blackhole-3d-xr/
- User (Adam) is building a blackhole 3D AR viewer and insists on WebXR approach

### Iteration History
- v1: AR.js + Three.js — CDN blocked in China
- v2: JSARToolKit5 — too complex
- v2.5: WebXR Device API — initially seemed unsupported
- v3: WebRTC + Three.js — universal but user rejected (wants WebXR)
- v4: WebXR Image Tracking — deployed but black screen, no error shown, camera not invoked
- v5: WebXR Multi-Strategy — **deployed** (commit `11bb54a`), key improvements:
  - All errors displayed on screen (no more silent black screen)
  - Auto-detect WebXR support on page load
  - Debug log panel at bottom showing step-by-step execution + User Agent
  - Three-strategy fallback: immersive-ar+image-tracking → immersive-ar basic → inline
  - Clear state machine: idle → checking → loading → active/error/not-supported

### User Context
- Adam's phone: Huawei/Honor brand
- Strong preference for WebXR approach; rejected WebRTC fallback suggestion
- Awaiting test results from v5 — need screenshot with error details to continue debugging

### Technical Notes
- Files modified in v5: `src/pages/viewer/index.tsx` (full rewrite), `src/pages/viewer/viewer.scss`
- WebXR image-tracking is experimental, requires chrome://flags on most browsers
- immersive-ar requires HTTPS + compatible browser (Chrome Android, Samsung Internet, etc.)

---

### 11:10 - 动态版本号部署（解决缓存问题）

**问题**：用户之前遇到过 v4 部署后浏览器缓存旧版本代码的问题，要求"每次更新带版本号避免缓存"

**解决方案**：
- 在 webpack `defineConstants` 中注入 `VERSION` 常量，格式 `年.月.日.时分`（如 `2026.05.08.1110`）
- 在 viewer 页面标题栏显示版本号，用户一眼就能判断是否加载了最新版本
- 开发环境显示 'dev'

**修改文件**：
- `config/index.ts` — 添加 VERSION 到 defineConstants
- `src/pages/viewer/index.tsx` — 声明 `declare const VERSION: string` 并在标题使用

**部署结果**：
- commit: `3dd8ec2`
- 版本号: `2026.05.08.1110`
- 访问: https://e476663603.github.io/blackhole-3d-xr/

**后续**：每次构建自动生成新版本号，用户通过标题栏版本号判断缓存状态
# 2026-05-08 工作日志

### 08:17  -  AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常

### 10:27 - WebXR Image Tracking v4 部署完成

**用户反馈**：用户要求改回 WebXR 方案，使用 `image-tracking` feature 实现识别图检测

**技术实现**：
- WebXR Device API `image-tracking` feature
- `navigator.xr.requestSession('immersive-ar', { trackedImages: [{ image: ImageBitmap, widthInMeters: 0.15 }] })`
- `frame.getImageTrackingResults()` 获取追踪状态
- 模型位置根据 `result.imageSpace` 的 pose 更新

**浏览器兼容性**：
- Chrome Android 91+（需在 chrome://flags 启用 "WebXR Incubations"）
- Samsung Internet 部分版本支持
- Safari iOS ❌ 不支持 image-tracking

**部署结果**：
- 已推送到 gh-pages 分支：commit `1400d62`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebXR Image Tracking 实现
- `src/pages/viewer/viewer.scss` — DOM Overlay 样式

**技术迭代路径更新**：
- v3: WebRTC + Three.js — 通用但无 marker detection
- v4: WebXR Image Tracking — 标准 AR 方案，有 marker detection ✅ 已部署

**测试要求**：
1. Chrome Android 在 chrome://flags 启用 "WebXR Incubations"
2. 准备 Hiro marker 识别图（已部署在 images/Hiro.png）
3. 对准识别图后模型会自动定位到 marker 上

**待验证**：用户手机是否支持 WebXR Image Tracking，需实测确认
# 2026-05-08 工作日志

### 08:17 - AR功能调试

- AR 项目当前状态：摄像头已打开但模型未加载、识别图功能缺失，原因是之前去掉 AR.js 时把 marker 检测一并删除了
- 需要自行实现 Hiro marker 检测（canvas 逐帧比对图像特征）替代 AR.js，同时修复 FBX 模型加载流程

### 10:20 - Camera AR v3 部署完成

**问题根因**：用户手机浏览器不支持 WebXR Device API，调用 `navigator.xr.requestSession('immersive-ar', ...)` 抛出 `InvalidStateError`

**解决方案**：放弃 WebXR，改用 WebRTC + Three.js 纯 H5 方案
- `getUserMedia` 获取后置摄像头视频流
- `<video>` 全屏播放作为背景
- Three.js WebGL Canvas 透明叠加在视频上
- FBX 模型渲染在相机前方，带缓慢自转动画

**兼容性优势**：
- ✅ 所有现代浏览器（Chrome/Safari/Edge/Firefox）
- ✅ 微信内置浏览器
- ✅ Android / iOS 通用

**部署结果**：
- 已推送到 gh-pages 分支：commit `f0d4d02`
- 访问地址：https://e476663603.github.io/blackhole-3d-xr/

**修改文件**：
- `src/pages/viewer/index.tsx` — WebRTC Camera + Three.js AR 实现
- `src/pages/viewer/viewer.scss` — video/canvas 叠加布局样式

**技术迭代路径**：
- v1: AR.js + Three.js — CDN 被墙失败
- v2: JSARToolKit5 — 国内可用但实现复杂
- v2.5: WebXR Device API — 手机浏览器不支持
- v3: WebRTC + Three.js — ✅ 通用方案，已部署

**下一步**：用户手机测试，确认摄像头权限和模型显示正常

### 10:27 - WebXR Image Tracking v4 部署完成 & 测试失败

**用户反馈**：用户要求改回 WebXR 方案，使用 `image-tracking` feature 实现识别图检测

**技术实现**：
- WebXR Device API `image-tracking` feature
- `navigator.xr.requestSession('immersive-ar', { trackedImages: [{ image: ImageBitmap, widthInMeters: 0.15 }] })`
- `frame.getImageTrackingResults()` 获取追踪状态
- 模型位置根据 `result.imageSpace` 的 pose 更新

**部署结果**：
- 已推送到 gh-pages 分支：commit `1400d62`

### 10:30 - v4 测试结果：黑屏，无摄像头调用

**用户设备信息**（从截图判断）：
- 华为/荣耀手机（从 UI 风格和状态栏判断）
- 使用自带浏览器或 Chrome 访问 `76663603.github.io`
- 页面显示 "WebXR Image Tracking" 标题和"请瞄准并对准 Hiro 识别图"
- **点击启动 AR 后黑屏，完全没有调用摄像头**

**问题确认**：
1. WebXR `requestSession('immersive-ar')` 调用静默失败
2. 用户手机（华为/荣耀）的浏览器 **不支持 WebXR immersive-ar**
3. 即使是 Chrome Android，`image-tracking` 也是实验性功能，需手动开启 flags
4. 国产手机浏览器基本都不支持 WebXR AR

**结论**：WebXR 方案在用户设备上不可行，必须回到 WebRTC 方案

**下一步计划（v5）**：
- 回到 WebRTC Camera + Three.js 基础方案（摄像头 100% 可调用）
- 加入 JSARToolKit5 或自研 canvas 图像特征匹配实现 marker 检测
- 确保：① 摄像头能打开 ② 能检测 Hiro marker ③ 模型定位到 marker 位置
## 2026-05-08 Session Notes

### WebXR Blackhole AR Viewer - v5 Multi-Strategy Deployed
- Project repo: `e476663603/blackhole-3d-xr` on GitHub, deployed via gh-pages
- Live URL: https://e476663603.github.io/blackhole-3d-xr/
- User (Adam) is building a blackhole 3D AR viewer and insists on WebXR approach

### Iteration History
- v1: AR.js + Three.js — CDN blocked in China
- v2: JSARToolKit5 — too complex
- v2.5: WebXR Device API — initially seemed unsupported
- v3: WebRTC + Three.js — universal but user rejected (wants WebXR)
- v4: WebXR Image Tracking — deployed but black screen, no error shown, camera not invoked
- v5: WebXR Multi-Strategy — **deployed** (commit `11bb54a`), key improvements:
  - All errors displayed on screen (no more silent black screen)
  - Auto-detect WebXR support on page load
  - Debug log panel at bottom showing step-by-step execution + User Agent
  - Three-strategy fallback: immersive-ar+image-tracking → immersive-ar basic → inline
  - Clear state machine: idle → checking → loading → active/error/not-supported

### User Context
- Adam's phone: Huawei/Honor brand
- Strong preference for WebXR approach; rejected WebRTC fallback suggestion
- Awaiting test results from v5 — need screenshot with error details to continue debugging

### Technical Notes
- Files modified in v5: `src/pages/viewer/index.tsx` (full rewrite), `src/pages/viewer/viewer.scss`
- WebXR image-tracking is experimental, requires chrome://flags on most browsers
- immersive-ar requires HTTPS + compatible browser (Chrome Android, Samsung Internet, etc.)

---

### 11:10 - 动态版本号部署（解决缓存问题）

**问题**：用户之前遇到过 v4 部署后浏览器缓存旧版本代码的问题，要求"每次更新带版本号避免缓存"

**解决方案**：
- 在 webpack `defineConstants` 中注入 `VERSION` 常量，格式 `年.月.日.时分`（如 `2026.05.08.1110`）
- 在 viewer 页面标题栏显示版本号，用户一眼就能判断是否加载了最新版本
- 开发环境显示 'dev'

**修改文件**：
- `config/index.ts` — 添加 VERSION 到 defineConstants
- `src/pages/viewer/index.tsx` — 声明 `declare const VERSION: string` 并在标题使用

**部署结果**：
- commit: `3dd8ec2`
- 版本号: `2026.05.08.1110`
- 访问: https://e476663603.github.io/blackhole-3d-xr/

**后续**：每次构建自动生成新版本号，用户通过标题栏版本号判断缓存状态

---

### 11:33 - v5 测试结果：用户反馈无版本号、无摄像头

**用户反馈**："并没有看到版本号，而且也没调用摄像头"

**问题分析**：
1. 版本号没显示 → 可能是浏览器缓存了旧版本（无 VERSION 常量），或 `defineConstants` 未正确注入
2. 摄像头没调用 → 当前代码（v5）是纯 WebXR 方案，`navigator.xr` 在用户华为手机浏览器上不存在/不支持，所有3个策略都会失败，永远无法打开摄像头

**关键结论**：纯 WebXR 方案在用户设备上死路一条。WebXR 是浏览器厂商实现的功能，不是代码层面能解决的兼容性问题。用户的华为手机浏览器不支持 `navigator.xr` API。

**建议方案**：混合方案（WebXR → WebRTC 自动降级）
- 先检测 WebXR 支持 → 支持就用 WebXR（最佳体验）
- 不支持 → 自动切换 WebRTC `getUserMedia` 打开摄像头 + Three.js 渲染
- 保证所有设备都能打开摄像头、看到3D模型

**状态**：等待 Adam 确认是否接受混合方案
